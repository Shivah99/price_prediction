# Install gh-pages package if you haven't already
npm install --save-dev gh-pages

# Add these scripts to your package.json
# "predeploy": "npm run build",
# "deploy": "gh-pages -d build"

# Then run:
npm run deploy
